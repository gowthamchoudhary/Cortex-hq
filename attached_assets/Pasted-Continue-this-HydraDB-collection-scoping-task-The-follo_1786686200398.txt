Continue this HydraDB collection-scoping task. The following 7 files have already 
been edited to accept and use a --collection parameter, threaded through all 
context.ingest / context.status / context.list / context.relations / 
context.update_source_metadata calls:

graph/ingest_to_hydradb.py
resolution/entity_resolution.py
resolution/dedup.py
temporal/conflict_cascade.py
temporal/truth_discovery.py
reasoning/answer_question.py
reasoning/calibrate_threshold.py

Two things remain:

1. Create `schema/create_collection.py --collection <name>`:
   - Since the HydraDB SDK has no explicit collection-creation method (collections 
     are created lazily on first scoped write), this script should idempotently 
     "ensure" a collection exists by:
     a. Calling databases.collections(...) to list existing collections in the 
        hackhydra-track1 database
     b. If the requested collection name isn't already listed, write a small 
        marker record via context.ingest scoped to that collection — e.g. a 
        single Entity-type record with canonical_name="__collection_init__", 
        state="system", so the collection now exists and shows up in future 
        collections() listings
     c. If the collection already exists, print a message and do nothing (no 
        duplicate marker records)
   - Print confirmation either way: "Collection '<name>' ready" or 
     "Collection '<name>' already exists"

2. Add a collection selector to `explorer/knowledge_explorer.py`:
   - Add a text input or dropdown at the top of the Streamlit app for the user 
     to type/select which collection to browse (default: "benchmark-eval")
   - Thread that selected collection value into every HydraDB query call the 
     explorer already makes, the same way it was threaded through the other 
     7 files
   - The rest of the explorer's existing logic and layout should not change

After both are done, run:
python -m py_compile graph/ingest_to_hydradb.py resolution/entity_resolution.py resolution/dedup.py temporal/conflict_cascade.py temporal/truth_discovery.py reasoning/answer_question.py reasoning/calibrate_threshold.py schema/create_collection.py explorer/knowledge_explorer.py

Fix any syntax errors that come up during compile check. Do not run any live 
HydraDB calls or LLM calls — compile-check only.