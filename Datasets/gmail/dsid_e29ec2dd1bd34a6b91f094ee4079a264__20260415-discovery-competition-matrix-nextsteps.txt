Post-call: discovery + proposed parity tests

From: Priya Natarajan <priya_natarajan@redwood.inference>
To: Alex Carter <alex.carter@acmeai.com>
Cc: 
Date: Thu, 15 Apr 2026 14:12:00 -0400
Subject: Post-call: discovery + proposed parity tests

Hi Alex —

Thanks for taking the time today. Quick recap from our discovery call and a proposed path for the competitive/feature-parity evaluation you asked about. I attached a lightweight competition matrix to this note (acme_competition_matrix.xlsx) and a draft parity smoke-test plan.

Key takeaways from our call:
- Primary goal: validate that Redwood can match the hyperscaler endpoints you currently use for streaming (low-latency token streaming), reliable function/tool calling, and structured JSON outputs for downstream processing.
- Success metrics you mentioned: 95th percentile generation latency under 300ms for short responses (<=128 tokens), function call reliability >= 99% on signature/args, and structured output validation F1 >= 0.92 vs your golden set.
- Integration constraints: you run inference in us-east and route production traffic through a managed gateway; data residency needs are standard (no persistent storage of PII).
- Procurement: you asked for an indicative price range and what we need to run a short POC.

Proposed next steps (short, prioritized):
1) Run a 2-day parity smoke run focusing on three pillars: streaming throughput/latency, function-calling correctness, and structured-output fidelity. (I propose Thu-Fri next week.)
2) Redwood SE-led test plan: we (SE) will run canonical requests from your golden prompt set and a baseline run against your hyperscaler endpoints; we'll capture tokens, latencies, and structured-output diffs.
3) Quick pricing note: I'll share a preliminary quote for a short reserved window + streaming test capacity (attached: redwood-pricing-quote.pdf).
4) Procurement: loop procurement@acmeai.com for the quick test agreement/NDA if needed.

Attachments: acme_competition_matrix.xlsx; parity_smoke_tests.md; redwood-pricing-quote.pdf

Can you confirm: (a) permission to run the parity tests against a staging API key you can provide, (b) preferred dates/time windows next week, and (c) whether procurement needs an internal PO or a simple MSA/LOI to proceed?

Thanks — I'll draft a short mutual action plan once I have your answers.

Best,
Priya
Account Executive | Redwood Inference
priya_natarajan@redwood.inference
+1 (415) 555-0182

From: Alex Carter <alex.carter@acmeai.com>
To: Priya Natarajan <priya_natarajan@redwood.inference>
Cc: procurement@acmeai.com
Date: Thu, 15 Apr 2026 15:02:00 -0400
Subject: Re: Post-call: discovery + proposed parity tests

Thanks Priya — really helpful summary. A few clarifications:

- We can give you a staging API key that mirrors our production routing for the test. I will create credentials and drop them in a secure share once procurement signs off.
- Dates: Fri, Apr 23rd (US business hours) works best for the live smoke run. We have two engineers who can join the kickoff (me + SRE lead).
- Procurement: procurement@acmeai.com — we typically do a short SOW + one-week PO for vendor POCs. Cost cap for the POC is $8k; happy to confirm if your pricing will fit under that.

A couple of ask/priority tweaks from our side:
1) Please prioritize function-calling correctness — we have several critical flows that auto-execute downstream tools and need near-zero format drift.
2) For streaming, we care most about consistent per-token latency (not just average) and the ability to resume/continue a stream after transient errors.
3) For structured outputs, we have a 300-example golden set; can you run both strict JSON schema validation and a fuzzy semantic checker?

If you can email a short test plan and list of exact artifacts/data you need (golden set, example tool signatures), I’ll route those internally. Thanks — looking forward to it.

—Alex
Senior Product Manager, AcmeAI

From: Priya Natarajan <priya_natarajan@redwood.inference>
To: aditya_rao@redwood.inference, laura_bennett@redwood.inference
Cc: 
Date: Fri, 16 Apr 2026 09:04:00 -0400
Subject: Fwd: Post-call: discovery + proposed parity tests

Folks — forwarding Alex's confirmation from AcmeAI. High level asks and what I need from SE/Eng: 

Begin forwarded message:

> From: Alex Carter <alex.carter@acmeai.com>
> To: Priya Natarajan <priya_natarajan@redwood.inference>
> Cc: procurement@acmeai.com
> Date: Thu, 15 Apr 2026 15:02:00 -0400
> Subject: Re: Post-call: discovery + proposed parity tests
>
> Thanks Priya — really helpful summary. A few clarifications: ...

My summary + asks for the team:
- Scheduling: Acme prefers Fri Apr 23 for the smoke run kickoff. We should block 90 minutes for kickoff + 4 hours of test window (can be spread across the day).
- Data/artifacts: they will provide a staging API key and a 300-example golden set. Alex asked for strict JSON schema checks + fuzzy semantic checks.
- Priorities they care about (in order): function-calling correctness, streaming per-token p95/p99, structured-output F1.
- Procurement: expect a one-week PO + SOW and $8k cap for the POC. Laura — can you confirm approval to proceed with a capped POC under $8k and any legal/finance flag for our standard demo SOW?

What I need from you (Aditya):
1) Can you prepare a concise parity smoke-tests doc that lists: test vectors, request load profile (concurrent streams), metrics to capture, and pass/fail thresholds? Aim for a 1-pager we can share with Acme. Link: parity_smoke_tests.md (attached in original thread).
2) Infrastructure ask: list the instance types and token budget (approx) we'll need for the 2-day run so I can validate pricing vs their $8k cap.
3) Quick runbook for streaming resume behavior and a small harness to compare function call args vs expected schema.

Let's aim to send the SE test doc to Alex by EOD today so procurement can start paperwork. If you can't make EOD, send a draft with bullet points.

Thanks — Priya


From: Aditya Rao <aditya_rao@redwood.inference>
To: Priya Natarajan <priya_natarajan@redwood.inference>, laura_bennett@redwood.inference
Cc: 
Date: Fri, 16 Apr 2026 10:02:00 -0400
Subject: Re: Fwd: Post-call: discovery + proposed parity tests — SE plan

Got it — attaching the parity smoke-tests doc and a short runbook (parity_smoke_tests.md). Summary below:

Proposed quick plan (1-pager summary):
- Kickoff (90 min): align success criteria, collect golden set + tool signatures, wire up staging API key.
- Smoke run (4 hours): 3 lanes executed in parallel but measured independently:
  1) Streaming lane: 50 concurrent short streams (<=128 tokens), steady state for 30 mins, capture p50/p95/p99 token latency, and simulate 10 transient disconnects to test resume.
  2) Function-calling lane: 300 examples from their golden set, run at 5 rps, verify exact arg serialization + post-call side effects in our harness.
  3) Structured-output lane: 300 examples validated against strict JSON schema + fuzzy semantic similarity (embedding-based) to compute F1/precision/recall.

Pass/fail thresholds (per Alex's asks):
- Streaming: p95 token latency <= 300ms; resume success >= 98%
- Function calls: 99% exact match on arg names/types; 99.5% total invocation success
- Structured outputs: strict-schema pass rate >= 92%; fuzzy semantic F1 >= 0.92

Infra & budget estimate for 1 day active testing (approx):
- Reserved instances: 2x A100-equivalent for parallel lanes (streaming + function heavy) for the day; spot for extra load if we need scale.
- Token budget: ~2.5M tokens for the test window (streams + functions + diffs).
- Estimated cost to Redwood (runs): ~$3.2k day 1 (incl. infra + monitoring) — our proposed quote keeps them under the $8k cap for 2 days including engineering time.

Deliverables by EOD today: parity_smoke_tests.md (attached), small harness repo link (internal), and a short kickoff agenda.

I can share the 1-pager with Alex now; Priya, do you want me to send directly to Alex (cc you) or hand off to you to send from AE?

Thanks — Aditya
SE, Redwood Inference
aditya_rao@redwood.inference

