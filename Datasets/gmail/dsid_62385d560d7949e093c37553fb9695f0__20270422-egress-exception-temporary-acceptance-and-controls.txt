Temporary outbound access request for third-party validator — proposed controls and acceptance

From: Alice Wong <alice.wong@edgehealth.com>
To: Jonas Weber <jonas.weber@redwood.ai>
Cc: edgehealth-security@edgehealth.com
Date: Thu, 22 Apr 2027 09:12:00 -0700
Subject: Request: temporary outbound access for validator service

Hi Jonas,

Thanks again for standing up the private deployment for our PoC. One outstanding item before we can run our third-party validation suite: the validator needs outbound access to a small set of external endpoints (S3 dataset buckets and the vendor telemetry endpoint) to fetch test vectors and post results. Our security team is asking for a formal, time-bound egress exception so they can approve the run.

Can you confirm whether Redwood can accept a scoped, temporary egress exception limited to: 
- dst: s3.us-west-2.amazonaws.com (GET)
- dst: validator-results.edgevalidator.com (443 POST)
- dst: updates.edgevalidator.com (443)

Ideally we get this for a 14-day window starting next Monday. Attached is the validator spec and a brief justification. We can provide the vendor IPs (they run from a handful of stable egress IPs) if you need them to configure the firewall.

Attachments: third-party-validator-specs.pdf

Thanks — Alice
Alice Wong
Security Engineer, EdgeHealth

From: Jonas Weber <jonas.weber@redwood.ai>
To: Alice Wong <alice.wong@edgehealth.com>
Cc: Laura Bennett <laura.bennett@redwood.ai>, Stephanie Nguyen <stephanie_nguyen@redwood.ai>
Date: Thu, 22 Apr 2027 10:01:00 -07:00
Subject: Re: Request: temporary outbound access for validator service

Hi Alice —

Appreciate the heads-up and the spec. We can support the PoC but need to land concrete controls before granting egress. High-level proposal from our side:

1) Timebox the exception to a hard 14-day window (start/end timestamps enforced by policy).
2) Allow only TCP/443 to the explicit hostnames/IPs you give; no wildcard domains.
3) Route the traffic through our egress proxy with mTLS and mutual auth to the vendor (we require vendor certs).
4) Enable connection-level logging + flow logs exported to our Splunk project for the duration.
5) Block all DNS/HTTP redirects — egress will be hostname+IP pinned.
6) Tokenized short-lived authorization: vendor must present signed outbound manifest (HMAC) per session; we will rotate the key every 48 hours.

Operational asks from you: provide final set of IP addresses (not more than eight), the certificate thumbprint for validator-results.edgevalidator.com, and a statement-of-work / POA&M listing the start date.

Laura / Stephanie: can you draft the firewall rule CSV and the exception form we require? I want to include explicit rollback steps (remove NAT entries, revoke mTLS certs).

On Fri, Apr 22, 2027 at 09:12, Alice Wong <alice.wong@edgehealth.com> wrote:
> Hi Jonas,
>
> Thanks again for standing up the private deployment for our PoC. One outstanding item before we can run our third-party validation suite: the validator needs outbound access to a small set of external endpoints...

—Jonas
Jonas Weber
Head of Private Deployments, Redwood Inference

From: Laura Bennett <laura.bennett@redwood.ai>
To: Jonas Weber <jonas.weber@redwood.ai>, Stephanie Nguyen <stephanie_nguyen@redwood.ai>
Cc: Alice Wong <alice.wong@edgehealth.com>
Date: Thu, 22 Apr 2027 12:08:00 -07:00
Subject: FW: Request: temporary outbound access for validator service — proposed rules and rollback

Folks —

Drafted the minimal firewall rule set and the formal exception packet. Key points in the CSV (attached):
- allow egress from private-pod-subnet (10.4.12.0/24) to explicit vendor IPs over 443 only; TCP ports other than 443 blocked.
- NAT mapping: ephemeral source IP assigned from dedicated NAT pool (138.91.210.16/28) so vendor can pin our outbound address.
- ACLs: deny by default, allow-list entries only.

Required controls to sign-off:
- Flow logs forwarded to splunk://redwood/edgehealth-poc (retention 90d).
- Packet sampling at 1:100 for deeper audit (we'll run capture if an incident is detected).
- Automated rule expiry: rules applied with a TTL; on expiry firewall manager removes entries and revokes NAT leases.
- CRITICAL: vendor certificate fingerprint checked on handshake; if mismatch we terminate.

Timebox: I recommend start 2027-04-26T08:00:00-07:00, end 2027-05-10T08:00:00-07:00 (14 days).

Attachments: fw-rules-proposal.csv, egress-exception-form.pdf

On Thu, Apr 22, 2027 at 10:01, Jonas Weber <jonas.weber@redwood.ai> wrote:
> Hi Alice —
>
> Appreciate the heads-up and the spec. We can support the PoC but need to land concrete controls before granting egress. High-level proposal from our side:...

—Laura
Laura Bennett
Senior Network Security Manager, Redwood Inference

From: Alice Wong <alice.wong@edgehealth.com>
To: Laura Bennett <laura.bennett@redwood.ai>, Jonas Weber <jonas.weber@redwood.ai>
Cc: edgehealth-security@edgehealth.com
Date: Thu, 22 Apr 2027 14:03:00 -07:00
Subject: Re: Request: temporary outbound access for validator service — proposed rules and rollback

Thanks — Laura / Jonas, this looks good and it's acceptable to EdgeHealth security.

Vendor provided the following stable egress IPs (please use these for the allow-list):
- 52.86.33.14
- 52.86.33.15
- 35.161.44.9

Certificate thumbprint for validator-results.edgevalidator.com: SHA256: 3F:8A:9C:...:44 (truncated here; full fingerprint in attached signed-exception.docx).

We have signed the egress-exception form and pushed it to DocuSign: https://app.docusign.com/sign/EDGE-EXC-2027-0422. Please confirm once rules are applied and the NAT mapping assigned so we can schedule the run.

Attachments: signed-exception.docx

Best,
Alice

