ACME inference qualification: SLO targets & benchmark plan

From: Rachel Kim <rachel.kim@redwood.ai>
To: Tom Reynolds <tom@acme.com>
Cc: Marissa Cole <marissa.cole@redwood.ai>, Marcus Lin <marcus.lin@redwood.ai>
Date: Sun, 30 Aug 2026 14:05:00 -0400
Subject: ACME inference qualification: SLO targets & benchmark plan

Tom — great talking earlier. Quick recap from my side and proposed next steps so everyone is aligned.

Call highlights:
- Primary SLOs: latency (p50/p95) for 1) interactive chat and 2) streaming token delivery, throughput targets for bulk batch embedding jobs, and concurrency targets for peak traffic.
- Streaming: you stated streaming must start delivering tokens within 60ms of generation for the UI spinner to feel responsive; p95 for first-byte ~200ms.
- Concurrency: production bursts up to 300 concurrent chat sessions with 20 req/s per session at peak was called out (we flagged this as high but doable with autoscale).
- Throughput: embedding jobs are ~10M rows/day; expect parallel workers and long-running batched jobs.

We attached the SLO worksheet you shared (acme_slo_requirements.xlsx) and our proposed benchmark harness draft (redwood_benchmark_harness_readme.md).

Proposed immediate next steps:
1) Marissa to confirm prompt mix and representative prompt set from Acme (Tom / Sarah to share a 200-example sample).
2) Marcus to propose a reproducible benchmarking recipe: hardware, model pin/version, warmup, measurement windows, seeding and prompt randomization.
3) Schedule a 90-minute technical run-through next week to validate the harness and seed data.

Can you confirm the streaming SLA expectations above (first-byte and token cadence), and upload a sample prompt set (JSONL or CSV) to the Drive link? Once we have that, Marcus will prepare the run recipe and we’ll scope time and infra.

Thanks — Rachel

--
Rachel Kim
Enterprise Sales | Redwood Inference
rachel.kim@redwood.ai | +1 (415) 555-0131

From: Tom Reynolds <tom@acme.com>
To: Rachel Kim <rachel.kim@redwood.ai>
Cc: Sarah Jenkins <sarah.jenkins@acme.com>, acme-prodops@acme.com
Date: Sun, 30 Aug 2026 15:12:37 -0400
Subject: Re: ACME inference qualification: SLO targets & benchmark plan

Thanks Rachel — that summary looks right. A couple clarifications from ProdOps side:
- Streaming: first-byte latency target is 60ms for short responses (~<64 tokens) at p50; p95 for first-byte we can accept up to 250ms if network overhead spikes. We also need token spacing consistency (no >300ms gaps between token emissions in normal conditions).
- Concurrency: the 300 concurrent chat sessions figure is peak for North America; normal steady-state is ~70 concurrent. We can provide time-of-day curves.
- Throughput: embedding pipeline runs nightly; batch windows are flexible but we prefer maximizing throughput vs cost during off-peak hours.

I’ve uploaded a 200-example prompt sample to the Drive folder and attached a short sheet with traffic curves. Link: https://drive.google.com/drive/folders/1A2b3Cacme-slo

Tom Reynolds
Head of Platform Engineering, Acme Corp

From: Marissa Cole <marissa.cole@redwood.ai>
To: Rachel Kim <rachel.kim@redwood.ai>, Marcus Lin <marcus.lin@redwood.ai>
Cc: Tom Reynolds <tom@acme.com>
Date: Mon, 31 Aug 2026 09:03:21 -0400
Subject: Re: ACME inference qualification: SLO targets & benchmark plan

Adding a few concrete asks from the SE perspective so Marcus can scope the harness:
- Prompt mix: need labels for prompt types (instruction, multi-turn, short-reply, long-form, code). If Acme can tag the 200-sample set that will help compute weighted latency distributions.
- Measurement definitions: we should run both cold-start and warm-state scenarios; define warm-state as after 5 sequential requests per model shard in the same workload to populate KV caches.
- Streaming checks: we propose measuring first-byte, median token inter-arrival, and max gap in a sliding 1s window. Also capture SSR (server-side retransmit) events if they occur.
- Reproducibility: record model commit/id, quantization profile, runtime kernel versions, hardware (GPU type + driver), container image hash, and the exact invocation payloads.

I’ll draft a checklist and a suggested harness config and paste below. Marcus — can you confirm whether we should include warmup of 1000 tokens per worker and a 5-minute measurement window?

Marissa
Senior Solutions Engineer | Redwood Inference

From: Marcus Lin <marcus.lin@redwood.ai>
To: Marissa Cole <marissa.cole@redwood.ai>, Rachel Kim <rachel.kim@redwood.ai>
Cc: Neha Kapoor <neha.kapoor@redwood.ai>, Tom Reynolds <tom@acme.com>
Date: Mon, 31 Aug 2026 09:18:47 -0400
Subject: Re: ACME inference qualification: SLO targets & benchmark plan

Thanks — here is a reproducible benchmark recipe I propose (high level):

1) Environment & hardware
- GPU: NVIDIA A100 40GB (pinned), driver 525.89, CUDA 11.8, cuDNN 8.5.
- Runtime: redwood-inference-runtime v3.4.1 (image digest: sha256:abc123...).
- Model: open-model-xyz v2.1, tokenizer v1.7, quantization: FP16 (no further compression) for this run.

2) Input sets
- Use Acme sample JSONL (200 examples). Expand to 2k by deterministic sampling with seed 42 (shuffle order but keep label distribution). Provide both the expanded workload and the original 200 as artifacts.

3) Warmup & measurement windows
- Warmup: for warm-state runs, each worker will run 1,000 tokens of synthetic warmup aligned to prompt type (to populate KV cache). Cold-start runs: start from empty KV and measure the first request latency separately.
- Measurement: run sliding windows of 5 minutes per test condition; collect per-invocation p50/p90/p95 latencies, first-byte timings for streaming, token inter-arrival histograms, throughput (tokens/s), GPU utilization and memory tail. Repeat each condition 3x with full environment reset between repeats.

4) Concurrency & throughput simulation
- Use a mix of virtual clients to simulate 70 steady concurrency and a 300-concurrency peak test. For chat sessions, simulate 20 req/s per session as indicated. For embedding jobs, run batched jobs with batch sizes 1k/2k/4k to measure throughput curves.

5) Determinism & reproducibility
- Fix seeds for any internal sampling (seed=42); log all random seeds used. Capture runtime logs, kernel versions, and perf counters. Archive harness config, prompt sets, and raw telemetry in the Drive folder and a per-run tarball.

6) Metrics & artifacts
- Produce: latency tables (p50/p90/p95), first-byte & token cadence charts, throughput vs batch size, flame graphs for hot kernels, and a short writeup noting any non-deterministic variance.

I’ll put this as a template in the repo (https://github.com/redwood-inference/benchmark-harness/tree/acme-scoped). If that looks good I’ll provision a small dedicated cluster for the initial runs and share timing.

—Marcus
Performance Engineer | Redwood Inference

From: Tom Reynolds <tom@acme.com>
To: Marcus Lin <marcus.lin@redwood.ai>, Marissa Cole <marissa.cole@redwood.ai>, Rachel Kim <rachel.kim@redwood.ai>
Date: Mon, 31 Aug 2026 10:07:54 -0400
Subject: Re: ACME inference qualification: SLO targets & benchmark plan

Appreciate the detail Marcus. A few follow-ups from our side:
- The A100 baseline is fine. Please also capture a NVMe+warm-cache scenario since our on-prem has a mixed storage path.
- For reproducibility, can you include a quick checklist for reproducing the run locally (we have a small infra team that will want to re-run on-site)?
- Timeline: if you can provision infra this week, we can do an initial run Thurs/Fri and provide feedback. Otherwise next week also works.

I uploaded the labeled 200-sample jumpset (acme_prompt_set_sample_v1.jsonl) to the Drive link. Moving forward — let’s aim to lock the measurement recipe and have a dry run by Friday.

Tom

From: Rachel Kim <rachel.kim@redwood.ai>
To: Tom Reynolds <tom@acme.com>
Cc: Marissa Cole <marissa.cole@redwood.ai>, Marcus Lin <marcus.lin@redwood.ai>, Neha Kapoor <neha.kapoor@redwood.ai>
Date: Mon, 31 Aug 2026 11:42:00 -0400
Subject: Re: ACME inference qualification: SLO targets & benchmark plan

Great — summary and next steps:
1) Acme: confirm labels on the 200 sample (done) and upload any additional corner cases. (Action: Tom / Sarah)
2) Redwood: provision a scoped A100 cluster and share a "how-to-reproduce" checklist and the harness config in the repo. (Action: Marcus)
3) SE/PD: run initial dry run Thu-Fri, capture artifacts and share results + raw telemetry link. (Action: Marissa / Marcus)
4) Follow-up meeting: 60–90 minute technical sync Friday 08/31 @ 16:00 ET (I’ll send invite).

Attachments: acme_slo_requirements.xlsx, acme_prompt_set_sample_v1.jsonl, redwood_benchmark_harness_readme.md

Thanks everyone — looking forward to the dry run.

Rachel
--
Rachel Kim
Enterprise Sales | Redwood Inference
