Post-call: Propulsion Labs discovery — deployment topology & SLO summary

From: Ava Chen <ava.chen@redwoodinference.com>
To: Samir Patel <samir@propulsion.ai>, Lena Ortiz <lena.ortiz@propulsion.ai>
Cc: Karthik Iyer <karthik_iyer@redwoodinference.com>, Marissa Cole <marissa_cole@redwoodinference.com>
Date: Wed, 07 Jun 2028 14:23:00 -0700
Subject: Post-call recap: deployment topology & SLO summary

Hi Samir / Lena —

Thanks again for the productive call. Quick recap and proposed next steps so we all have the same picture. I tried to capture the technical constraints you emphasized and the choices we discussed for a low-latency conversational recommendation use case. Please flag anything I missed.

Key pain points and goals
- 50 ms p95 token latency target for short-turn chat responses (average payload ~40 tokens).
- Sustained throughput target: 800 qps during peak batch windows; typical baseline 120 qps.
- Must run in a customer VPC (Private deployment) due to payer-data restrictions; SOC 2 evidence required.
- Deterministic cost / predictable capacity for budgeting — looking at reserved pool economics.

Model & inference constraints we captured
- Priority model: Llama-3-13B (you mentioned an in-house fine-tune) — testing quantized int8 first, fallback to fp16 if quality regressions occur.
- KV cache warm-up important: cold-start across shards will be visible; want auto-prefetch behavior discussed on call.
- Prompts include medium-length context and user history; average seq len ~512 tokens, rare long-context queries to 2048.

Topology options (high level)
- Dedicated private pool inside your VPC (preferred): multi-AZ, 4xA100-equivalent nodes, autoscale based on p95 latency; KV cache per node with cross-node invalidation TTL.
- Hybrid: hosted control plane + customer-managed inference nodes (we ship the runtime into your environment) — faster ops onboarding, still keeps keys in your KMS.

Immediate asks / next steps
1) Propulsion: share a representative 24-hour request trace and 30 anonymized prompt examples (we need token distribution and cold-start patterns) — see attachment placeholders.
2) Redwood: we will run a quick microbench on an internal replica to validate p95 latency with int8 on similar hardware and share SLO matrix.
3) Schedule tech deep-dive with infra + security (30–45m) next week — propose Tue/Wed window.

Attachments included for reference: Propulsion_SLO_matrix.xlsx (SLO targets + ask matrix), kv-cache-latency-profiles.pdf (example telemetry we’ll want), propulsion-map.json (proposed VPC connectivity sketch).

Thanks — Ava
Ava Chen
Solutions Engineer, Redwood Inference
ava.chen@redwoodinference.com

---

(Onwards: quoted original call notes available on the shared Drive link above.)
From: Samir Patel <samir@propulsion.ai>
To: Ava Chen <ava.chen@redwoodinference.com>
Cc: Lena Ortiz <lena.ortiz@propulsion.ai>, Karthik Iyer <karthik_iyer@redwoodinference.com>
Date: Wed, 07 Jun 2028 14:35:12 -0700
Subject: Re: Post-call recap: deployment topology & SLO summary

Thanks Ava — solid summary, appreciate the clarity. Couple clarifications below: 

- Latency target: 50 ms p95 applies to the token generation portion only; we have separate pre-processing that adds ~8–12ms. So end-to-end target is ~60–65ms p95.
- For throughput: 800 qps is our absolute peak for promo events; typical headroom we need is 300–400 qps.
- We will provide a scrubbed trace by EOD Friday and the prompt samples next Monday.

One constraint we didn’t emphasize: our security team requires that any proprietary weights never leave our subnet in unencrypted form; if Redwood is shipping a runtime, we need verification of secure enclave / pointer to HSM integration.

Lena — can you confirm whether Propulsion legal prefers a mutual NDA before sharing traces?

—Samir
Head of ML Ops, Propulsion Labs

From: Karthik Iyer <karthik_iyer@redwoodinference.com>
To: Samir Patel <samir@propulsion.ai>, Ava Chen <ava.chen@redwoodinference.com>, Marissa Cole <marissa_cole@redwoodinference.com>
Date: Wed, 07 Jun 2028 15:02:30 -0700
Subject: Re: Post-call recap: deployment topology & SLO summary

Samir / team —

Thanks for the clarifications. A few technical notes and concrete asks from the Redwood infra side so we can size the dedicated pool and KV cache strategy: 

1) Hardware & perf assumptions (what we’ll validate in the microbench):
- Llama-3-13B quantized int8 on 80GB-class accelerators: we expect ~40–55 ms p95 token time for 40-token responses when the KV cache is warm. Cold-start (first request after shard spin-up) can be 2–3x unless we pre-warm KV.
- To meet your 50 ms p95 generation-only target consistently at 300 qps baseline, recommendation: 3 service shards with cross-shard routing + 1 hot-standby. For 800 qps peak, plan to scale to 6 shards.

2) KV cache strategy:
- Per-worker local KV cache with a Redis-backed global index for invalidation and TTL. Warm-up strategies: proactive prefetch on session creation + on-access async prefetch.

3) Networking & VPC notes:
- We can deploy the runtime inside your subnet. For weights, two options: a) weights remain on customer-managed storage (recommended) and are memory-mapped at runtime; b) Redwood-managed image with attestation and HSM key wrapping. We will prepare an HSM/KMS integration playbook.

4) Telemetry / SLO measurement:
- We will instrument token-level latency, KV hit/miss rate, and cold-start counter. Please confirm the set of tracing headers you expose so we can align span propagation.

Next steps from Redwood infra: we’ll run a controlled microbench against a representative workload (using your scrubbed traces) and deliver: 
- Measured p50/p95/p99 token latencies for int8 and fp16 variants
- KV cache hit-rate sensitivity and estimated steady-state CPU/GPU and memory footprint
- Cost estimate for reserved capacity (monthly) vs burst usage model

I’ll attach a short checklist for the microbench (will send as Propulsion_SLO_matrix.xlsx).

—Karthik
Senior Solutions Architect, Redwood Inference

From: Lena Ortiz <lena.ortiz@propulsion.ai>
To: Ava Chen <ava.chen@redwoodinference.com>, Karthik Iyer <karthik_iyer@redwoodinference.com>
Cc: Samir Patel <samir@propulsion.ai>, Marissa Cole <marissa_cole@redwoodinference.com>
Date: Wed, 07 Jun 2028 16:12:04 -0700
Subject: Re: Post-call recap: deployment topology & SLO summary

Ava / Karthik —

Confirmed. Legal will send a short NDA template this afternoon; we can’t share traces until that’s signed. Expect the scrubbed trace and prompt samples within 48 hours after NDA.

On the HSM note: we prefer option (a) if possible (weights on our managed storage), but we’re open to the attestation workflow for faster onboarding if you can show the HSM playbook. Please make that playbook available before the technical deep-dive.

Proposed slot for deep-dive: Wed, Jun 14 at 10:00 PT (30–45m). If that works, send calendar invite.

Thanks — Lena
VP, Security & Ops, Propulsion Labs

---

(Attachment placeholders referenced earlier: Propulsion_SLO_matrix.xlsx, kv-cache-latency-profiles.pdf)