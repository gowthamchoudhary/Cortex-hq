Sizing input: peak burst profiles, queueing assumptions, and SLO tradeoffs

From: Ari Rosen <ari@lumina.ai>
To: jonas_weber@redwood.ai
Cc: ops@lumina.ai
Date: 2028-05-03T09:12:00-07:00
Subject: Quick: peak burst profile for Dedicated sizing

Hey Jonas,

Thanks again for the demo yesterday — the team is excited. Our procurement lead asked for a conservative sizing estimate we can bake into the contract so we can get to a PO quickly. We need guidance on two specific things:

1) Peak burst behavior: we expect occasional marketing-driven spikes (campaign launches) that produce 10x baseline traffic for ~30-90 minutes. What's a prudent queueing/backlog policy we should assume so we don't overprovision?

2) Tail latency SLO tradeoffs: our product team cares about 99th percentile tail latency for short (<=128 token) responses, but legal wants a cost cap. We need a mapping between concurrent RPS, expected p99 latency, and approximate GPU hour burn per day.

I've attached a very small CSV with their synthetic peak sample and a short PDF with QoS asks. Can you confirm what minimal dataset you need from our side to produce a sizing sketch and a likely Dedicated footprint (GPU count / SKU)?

Appreciate it — Ari
Ari Rosen | Head of Platform | Lumina AI

From: jonas_weber@redwood.ai
To: Ari Rosen <ari@lumina.ai>
Cc: dev_patel@redwood.ai, marissa_cole@redwood.ai
Date: 2028-05-03T11:02:00-07:00
Subject: Re: Quick: peak burst profile for Dedicated sizing

Ari —

Thanks for the clear asks and the attachments. We can put together a short conservative footprint sketch. To make it useful and avoid rework, please confirm/provide the following minimal inputs (we can work from estimates if needed):

- steady_state_qps (average RPS during non-campaign periods) and baseline_concurrency target (typical concurrent sessions)
- expected_peak_factor (you suggested 10x) and expected_peak_duration distribution (30m/60m/90m buckets)
- breakdown by workload type: realtime short-completion (<=128 tokens), mid (129-512 tokens), long (>512 tokens) — percent of total at peak and baseline
- average prompt length and average generated length per class (tokens)
- desired latency targets per class (p50/p95/p99) — call out which must be strict vs. negotiable
- KV cache hit rate expectation (do you prefix cache similar users?) and session affinity requirements
- acceptable queueing behavior: max queue time before rejecting / degrade to async / fallback to smaller model
- expected requests/hour and business hours vs. 24x7 distribution (regionally concentrated?)
- any regulatory constraints that force private footprint or special KMS/HSM?

If you prefer, we can run with conservative defaults: steady_state_qps=25, peak_factor=10 for 45 minutes, workload mix=60% short / 30% mid / 10% long, avg prompt=40 tokens, avg response short=80 tokens, p99 target short=350ms. With those defaults we can deliver a 1-page sketch (GPU SKU + count + expected token/hr and cost ballpark).

I'll loop in Dev (technical sizing) and Marissa (capacity planning) so we can turn this around in 48 hours after you confirm or provide the CSV with hourly traces.

Cheers,
Jonas Weber
Senior Solutions Architect | Redwood Inference

On Thu, May 3, 2028 at 12:47 PM Dev Patel <dev_patel@redwood.ai> wrote:
From: dev_patel@redwood.ai
To: jonas_weber@redwood.ai, ari@lumina.ai
Cc: marissa_cole@redwood.ai
Date: 2028-05-03T12:47:00-07:00
Subject: Re: Quick: peak burst profile for Dedicated sizing

Ari / team —

Quick technical clarifications so we size the right SKU:

- Model variant: which family are you planning to serve? (larger decoder-only baseline like Llama-3-70B vs trimmed 13B). If unsure, tell us current model footprint in GB or vRAM.
- Max sequence length you expect in production (including prompt + response KV caching window).
- Do you need multi-tenant isolation per team or single-tenant dedicated pools? That affects packing density.
- Are you comfortable with quantized variants for cost reduction (int8/4)? Any regen-quality constraints?

Assumptions we will use unless you override: 13B int8 serving for short path, we target 160ms p50 and 350ms p99 at 1.5k concurrent tokens/sec on a 16xA100-like pool with continuous batching. For peak bursts we model 10x QPS sustained for 45 minutes with a burst queue cap set to 2500 requests and graceful degradation to a smaller 6B fallback when queue depth exceeds 80%.

I'll produce two footprints: conservative (no fallback, higher GPU count) and cost-aware (fallback + more aggressive batching).

-Dev
Principal Engineer, Serving Runtime | Redwood Inference

From: jonas_weber@redwood.ai
To: Ari Rosen <ari@lumina.ai>
Cc: dev_patel@redwood.ai, marissa_cole@redwood.ai
Date: 2028-05-04T16:48:00-07:00
Subject: Re: Quick: peak burst profile for Dedicated sizing

Ari —

Summary & next steps based on your sample + our defaults (attached draft sketches):

1) Minimal inputs received: sample CSV shows baseline ~30 RPS, campaign spikes up to ~320 RPS for ~40 minutes (matches your 10x note). No regional skew indicated.

2) Conservative footprint (no fallback):
- Assumes 13B int8 baseline models, p99 short target 350ms
- Recommended pool: 6 x GPU (A100-80GB class) with continuous batching and 3:1 request packing.
- Expected GPU-hours/day: 144 GPUh/day (24h * 6 GPUs).
- Expected monthly burn (ballpark compute cost only): ~4,320 GPUh/month.

3) Cost-aware footprint (fallback + queueing):
- 4 x GPU primary pool + 2 x smaller GPUs for fallback (6B).
- Queue cap: 2,500 requests; when queue depth >80% route new requests to fallback model with p99 ~700ms for short responses.
- Expected GPU-hours/day: ~110 GPUh/day.

4) Queueing policy recommendation:
- Allow burst queueing up to 45 minutes at 10x with graceful fallback after 5 minutes queued (to avoid unbounded latency).
- Reject after 10 minutes and surface 429 to clients with guidance to retry or use async endpoints.

5) Data we still need to finalize: actual per-request token distributions, confirmation of model family, and any hard p99 cutoffs for legal. If possible, send a 48-hour trace by bucket (timestamp,RPS,avg_prompt_tokens,avg_response_tokens,workload_tag).

Next steps: Marissa will convert these sketches to a short spreadsheet with SKU mapping and cost sensitivity scenarios; Dev will simulate expected p99 under the conservative vs cost-aware configs and share traces. We can have a 30-min call tomorrow to walk Lumina through tradeoffs.

Would Monday 10am PT work for a call?

Thanks — Jonas
Senior Solutions Architect | Redwood Inference
Redwood Inference | www.redwood.ai
Attachments: lumina_peak_sample.csv (sample), qos_requirements.pdf (lumina QoS doc)
