Admin event ledger extract request — SOC 2 evidence

Hi Mina,

We're kicking off the evidence collection for Acme Health's SOC 2 Type 2 review (period: 2025-10-01 to 2026-09-30). To complete the Admin Activity audit, we need a complete extract of admin/event logs that capture:

- Console admin actions (role changes, user invites, permission grants/revocations)
- API key creation/rotation/deletion events
- Model deployment actions (create/deploy/rollback) and associated commit ids
- Service account impersonation and token issuance events
- Audit log fields: timestamp (UTC), actor, actor_identity_type (user/service), action_type, resource_type, resource_id, success/failure, request_id, ip_address, request_payload_hash (if present)

Please confirm:
1) Retention window available for export (we expect 13 months plus current month)
2) Export formats supported (ndjson, CSV, Parquet) and max time-range per job
3) Any PII or token redaction applied before export and whether we can request redacted vs raw exports
4) A preferred mechanism to receive large exports (S3 presigned URL, GCS, or secure FTP)

Can we get an initial sample (first week of Oct 2025) schema + 24h of events to validate parsing? Acme's InfoSec asked for checksums on exports and an expected SLA of 3 business days for full-range pulls.

Thanks,
Liza Gomez
Senior Auditor — ThirdWave Audit

-----
Attachment: acme_malp_request_20261016.pdf (request matrix)

Hi Liza / Acme team,

Thanks — we can provide the event trail you describe. Quick answers up front:

1) Retention: we retain Audit & Admin events for 18 months in our hot store, with immutable cold snapshots for 36 months. For the requested period (2025-10-01 to 2026-09-30) we can pull full-range.
2) Formats: exports supported — ndjson (recommended), gzipped CSV, and Parquet (columnar) for analytics.
3) Redaction: by default, exports contain hashed request_payload (SHA256) and token placeholders; we can supply raw payloads under an NDA/attestation if Acme needs it. IP addresses are retained; we can provide a redacted-export variant with IP truncation if requested.
4) Delivery: S3 presigned PUT is preferred for large dumps (>10GB). We can also push to an SFTP endpoint.

For schema/validation, attaching our current admin_event_schema_v1.csv and a short export_instructions.md that covers filters and max time-range per job (7 days per job for ndjson streaming; larger ranges supported via bulk snapshot jobs).

Notes on scope and caveats:
- Deploy events include the model name, model hash, and deployment config, but not the binary model artifact.
- Cross-tenant operator actions (support staff assisting customers) are logged with operator ids and tenant context. We can filter to Acme tenant_id only.
- Log order is correlated by request_id; any retries will have the same request_id and a retry_count field.

I'll loop in Karthik to outline the exact export job; Laura, can your team accept S3 presigned uploads?

Regards,
Mina Haddad
Senior Manager, Security & Compliance
Redwood Inference

Attachments: admin_event_schema_v1.csv, export_instructions.md

Folks — technical follow-up / implementation notes.

- Export mechanics: we'll create a bulk snapshot job that streams JSON Lines (ndjson) compressed with gzip. Each line is an event JSON matching the CSV schema. We will include a manifest file with ranges and checksums (sha256).
- Time-range batching: to keep jobs within resource limits we'll split the 13-month range into monthly blobs by default; we can do custom ranges if ThirdWave requests larger single files.
- Filtering: add tenant_id=acme-tenant-42, event_type in (admin_console, api_key, deployment, service_account, impersonation).
- Redaction: raw_payload flag needs an ROA (Records of Access) approval; for auditors we will require an Acme-signed attestation that raw payloads will be handled per their policies.
- Data integrity: manifest includes file_name, start_ts, end_ts, event_count, sha256. We'll also provide a signed statement (PGP) if Acme requires non-repudiation.
- Retention note: although we keep 18 months hot, exports for SOC artifacts will be archived to a locked bucket for 36 months if requested. This is a billable operation (archive+hold).

I'll schedule the first sample extraction (2025-10-01 to 2025-10-07) for tomorrow morning PST and provide a presigned S3 PUT URL. Mina: confirm who at Acme should get the presigned URL and whether they want redacted IPs.

—Karthik
Senior Software Engineer, Serving Runtime

Thanks — confirming logistics from the compliance ops side.

- Acme point-of-contact for uploads: Jenna Park <jenna.park@acmehealth.com>. Please send the S3 presigned PUT and manifest to her.
- Redaction: Acme asked to keep IPs intact for correlation with their VPN logs. Send the unredacted export and mark the S3 bucket with the standard evidence retention label.
- Timeline: sample (7 days) needed by 2026-10-19 EOD; full 13-month export expected in two waves: months 1–6 by 2026-10-26, months 7–13 by 2026-11-09. If archive+hold is requested, raise a billing ticket to Finance (JIRA-SEC-721).
- I'll own manifest verification and handoff to ThirdWave. Priyom: please prepare the attestation template for raw_payload access and share with Acme legal.

Mina/Karthik — please proceed with the sample job and mark the threads with JIRA-SEC-721 for audit trail.

Laura Bennett
Director, Compliance Operations
Redwood Inference
