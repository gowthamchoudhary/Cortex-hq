Perf evaluation protocol & next steps (SLOs, prompts, benchmark run)

From: Laura Bennett <laura.bennett@redwood.com>
To: Elena Ortiz <elena@acme-analytics.com>
Cc: Rishi Malhotra <rishi.malhotra@redwood.com>, Marissa Cole <marissa.cole@redwood.com>
Date: Sun, 19 Jul 2026 15:03:00 -0700
Subject: Perf evaluation protocol & next steps (SLOs, prompts, benchmark run)

Hi Elena — great to meet you on the call today. Quick recap and proposed next steps so we have alignment before the benchmark run.

1) Customer SLOs you called out on the call
   - P95 latency <= 75 ms for 512-token requests (open to per-endpoint variation)
   - Sustained concurrency target: 200 concurrent infer requests
   - Sustained throughput target: ~1.2M tokens/minute across the pool
   - Streaming: token-level streaming with end-to-end first-byte within 20 ms for interactive flows

2) What we (Redwood) will contribute
   - Standardized eval harness (containerized) to reproduce runs
   - Request/response logging and per-token latency breakdowns
   - Reproducible seed prompt set and measurement definitions (we'll exchange prompt sets)

3) Asks from Acme for the run
   - A representative ~2000-example prompt set with traffic mix noted (interactive vs batch)
   - Any do-not-send PII or redaction rules for test payloads
   - Preferred run window (we suggested Wed 07/22 01:00–07:00 UTC for a full load test)

4) Next steps / owners
   - Elena: share prompt sample + procurement contacts (by Mon 07/20)
   - Laura: push benchmark harness + measurement spec to shared Drive and HubSpot (today)
   - Rishi: coordinate Dedicated capacity and cost estimate for sustained 200 concurrency (ETA Tue 07/21)
   - Marissa: define SLAs/rollout plan for production (post-eval)

Attachments: we'll exchange a simple prompt CSV template and an SLO matrix.

Thanks — Laura
Laura Bennett
Senior Solutions Engineer, Redwood Inference
laura.bennett@redwood.com
Redwood Inference


From: Elena Ortiz <elena@acme-analytics.com>
To: Laura Bennett <laura.bennett@redwood.com>
Date: Mon, 20 Jul 2026 09:12:00 -0400
Subject: Re: Perf evaluation protocol & next steps (SLOs, prompts, benchmark run)

Laura — thanks for the clear recap. Confirming the SLOs look right to us; a couple of clarifications:

- The 75 ms p95 target is for our primary search endpoint (semantic_search_v2) where median request sizes are ~320 tokens. For longer-generation endpoints (summarization_v1) we'd accept a looser target (p95 <= 250 ms for ~1024 tokens).
- Concurrency: the 200 concurrent is an average sustained target; spikes to 350 for short windows are expected during business hours.
- Streaming: interactive sessions (chat widget) must not exceed 25 ms per-token on average; 20 ms is desirable but not strict.

I've attached a small sample prompt set (acme_prompt_set_v1.csv) and an initial SLO matrix (acme_slo_matrix.xlsx). These are sanitized — no PII. Procurement is cc'd on this thread for contracting prep.

Elena Ortiz
Head of Platform Engineering, Acme Analytics
elena@acme-analytics.com

Attachment: acme_prompt_set_v1.csv (text/csv), acme_slo_matrix.xlsx (application/vnd.openxmlformats-officedocument.spreadsheetml.sheet)


From: Laura Bennett <laura.bennett@redwood.com>
To: Rishi Malhotra <rishi.malhotra@redwood.com>, Marissa Cole <marissa.cole@redwood.com>, Sean Gallagher <sean.gallagher@redwood.com>
Cc: Elena Ortiz <elena@acme-analytics.com>, Procurement <procurement@acme-analytics.com>
Date: Mon, 20 Jul 2026 11:03:00 -0700
Subject: Fwd: Perf evaluation protocol & next steps (SLOs, prompts, benchmark run)

Forwarding Acme's samples and the ask. Key items for the ops/benchmark team:

- We need to validate prompt distribution (interactive vs batch) and create workload mix file.
- Reproducibility: we'll run 3 independent trials, with seed RNG logged, identical model weights and quantization flags, and full request/response dumps (sanitized).
- Measurement definitions: p50, p95, p99 latencies at both request-level and per-token-level for streaming; token throughput per minute; error rates.

Proposed run window: Wed 07/22 01:00–07:00 UTC (covering off-peak and ramp). Rishi: can you confirm capacity and pricing for this single benchmark window (including burst up to 350) so we can share with procurement?

Laura


From: Rishi Malhotra <rishi.malhotra@redwood.com>
To: Laura Bennett <laura.bennett@redwood.com>, Marissa Cole <marissa.cole@redwood.com>, Sean Gallagher <sean.gallagher@redwood.com>
Cc: Elena Ortiz <elena@acme-analytics.com>
Date: Tue, 21 Jul 2026 06:18:00 -0700
Subject: Re: Perf evaluation protocol & next steps (SLOs, prompts, benchmark run)

Thanks — quick notes from capacity/ops side:

- Dedicated pool: we can reserve a test pool equivalent to 6 x A100-80GB (logical capacity) for the 6-hour window; should handle 200 sustained concurrency and short bursts to 350 with our autoscale buffer.
- Cost estimate for the window (incl. storage & logging): $6,400 (one-off benchmark engagement fee) — I'll drop a formal quote into HubSpot.
- Measurement harness: Sean will ship the containerized harness image to the Drive link Laura shared. The harness exposes 3 run modes: dry-run (check config), calibration (warmup + KV priming), and full-run (repro). Each run emits: per-request latencies, per-token latencies for streamed responses, and KV cache hit stats.

Sean — please confirm the harness version and list of flags we require from Acme (seed, traffic-mix file, model revision to pin).

Rishi
Head of Sales Engineering, Redwood Inference


From: Sean Gallagher <sean.gallagher@redwood.com>
To: Laura Bennett <laura.bennett@redwood.com>, Rishi Malhotra <rishi.malhotra@redwood.com>, Marissa Cole <marissa.cole@redwood.com>
Cc: Elena Ortiz <elena@acme-analytics.com>
Date: Tue, 21 Jul 2026 08:05:00 -0700
Subject: Re: Perf evaluation protocol & next steps (SLOs, prompts, benchmark run)

Confirming harness details:

- Harness image: redwood/benchmark-harness:v2.4.1 (container) — pushed to shared Drive with a small README.
- Required inputs from Acme: prompt CSV, traffic_mix.json (simple JSON with percentage splits), seed integer, model_revision tag (we can accept an LLM family + revision).
- Flags we'll set for reproducibility: --seed, --kv-prime (true/false), --tokenize-mode (bytepair|utf8), --streaming-enabled (true/false), --concurrency-target.
- Measurement output: combined CSV + JSON with p50/p95/p99, per-token latency histograms, throughput by minute, and a reproducible run manifest (image hash + flags + seed).

I'll add the harness README to the Drive folder now: https://drive.google.com/drive/folders/1a2b3c-Acme-Benchmark


From: Marissa Cole <marissa.cole@redwood.com>
To: Elena Ortiz <elena@acme-analytics.com>, Laura Bennett <laura.bennett@redwood.com>, Rishi Malhotra <rishi.malhotra@redwood.com>
Cc: Procurement <procurement@acme-analytics.com>
Date: Tue, 21 Jul 2026 09:18:00 -0700
Subject: Re: Perf evaluation protocol & next steps (SLOs, prompts, benchmark run)

Elena — thanks for the prompt CSV and SLO matrix. Two quick commercial items before the run:

1) I'll send a short SOW and one-off benchmark engagement quote (Rishi's number + brief scope) to procurement by EOD today.
2) Please confirm acceptance of our standard non-production data handling addendum (we won't retain payloads beyond 30 days from the run and we'll provide sanitized logs for analysis).

Operational next steps: once procurement signs off we'll schedule the harness run for Wed 07/22 01:00–07:00 UTC. Please reply-all with confirmation of: prompt CSV finalized, traffic-mix file (or approximate % interactive vs batch), and a point-of-contact for on-call issues during the run.

Best — Marissa
Account Executive, Redwood Inference
marissa.cole@redwood.com

