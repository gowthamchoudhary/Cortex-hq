Tenant isolation & local SSD sizing guidance for Pegasus on-prem deployment

From: Soojin Lee <soojin_lee@redwood.example.com>\nTo: Dev Patel <dev_patel@redwood.example.com>, Hannah Schmitt <hannah_schmitt@redwood.example.com>\nCc: \nDate: 2027-07-28T09:12:00Z\nSubject: Draft: tenant isolation + local SSD sizing for Pegasus on-prem\n\nHi team,\n\nAttaching an initial pass on tenant isolation and local SSD recommendations for the Pegasus pilot. High-level asks from the customer call: strict tenant isolation across workloads, fast local NVMe for KV cache and ephemeral model shards, and an easy recovery story for PVs across reboots.\n\nKey open questions for our engineering review:\n- Node pool separation: single shared GPU pool with per-tenant namespaces and CRD-based resource quotas vs fully isolated GPU node pools per tenant.\n- Local SSD persistence: use local PVs (CSI local static provisioning) for cache + ephemeral model slices, with periodic async replication to a shared NFS/CEPH for snapshots.\n- Multipath/iSCSI concerns for their SAN fallback during maintenance windows.\n\nPlease review the sizing tab in the attached spreadsheet (pegasus-localssd-sizing.xlsx) — I estimated 1.5 TB NVMe per GPU node to hold shard cache and model working set for their expected QPS.\n\nQuick asks: Dev, can you call out operational tradeoffs (node slice eviction, taints/tolerations, and podDisruptionBudget behavior)? Hannah, please draft the wording under "data retention & PV reclaim" that we can give to their ops team.\n\nThanks,\nSoojin\nSoojin Lee\nPrivate Deployments PM\nRedwood Inference\n
From: Dev Patel <dev_patel@redwood.example.com>\nTo: Soojin Lee <soojin_lee@redwood.example.com>, Hannah Schmitt <hannah_schmitt@redwood.example.com>\nDate: 2027-07-29T11:03:00Z\nSubject: Re: Draft: tenant isolation + local SSD sizing for Pegasus on-prem\n\nSoojin —\n\nQuick notes after skimming the spreadsheet and their queue profile:\n- Node pools: Recommend split GPU node pools per tenant for strong isolation. Use a small shared CPU node pool for control-plane services and batch jobs. That keeps noisy neighbor risk lower and allows per-tenant autoscaling.\n- Scheduling: Apply node taints (gpu=dedicated:NoSchedule) + per-tenant tolerations and nodeSelector labels like tenant=pegasus-<name>. This makes admission/audit simpler.\n- Eviction: local SSD-backed pods should use emptyDir or local PersistentVolumes depending on durability needs. For cache-only volumes (rebuildable from snapshot), set reclaimPolicy=Delete and tolerate eviction. For shards that are expensive to rebuild, use ReclaimPolicy=Retain + scheduled replication.\n- Storage: prefer CSI local PVs + a lightweight agent that snapshots to their shared object store every 15 minutes. Avoid NFS for high-I/O KV cache.\n\nI attached a short example net diagram and a sample Kubernetes StorageClass snippet in net-diagram-v2.png.\n\nI'll also note multipath/iSCSI: if they want SAN fallback across maintenance, plan for host-level multipath and ensure node OS is configured to survive SCSI resets — this is an infra ops task on their side, we can provide a checklist.\n\n—Dev\nDev Patel\nSenior Infra Engineer, Redwood Inference\n
From: Soojin Lee <soojin_lee@redwood.example.com>\nTo: John Smith <john.smith@pegasus.ai>, Infra Team <infra@pegasus.ai>\nCc: Dev Patel <dev_patel@redwood.example.com>, Hannah Schmitt <hannah_schmitt@redwood.example.com>, contracts@pegasus.ai\nDate: 2027-07-30T02:20:00Z\nSubject: Tenant isolation & local SSD sizing guidance (proposal)\n\nHi John / Pegasus Infra team —\n\nThanks for the call yesterday. Per our discussion, attached is a proposal for how Redwood would recommend configuring the on-prem cluster to meet your isolation, performance, and recoverability goals. Attachments: pegasus-localssd-sizing.xlsx, net-diagram-v2.png, redwood-pv-reclaim-policy.pdf.\n\nSummary of recommendations (short):\n- Isolation: dedicate GPU node pools per tenant (labelled tenant=<name>) + RBAC + admission webhook to prevent cross-namespace workload binding. Shared CPU pool for infra and batch.\n- Performance: local NVMe per GPU node (1–2 TB depending on working set); use CSI local PVs for high-throughput ephemeral state. Avoid NFS for KV cache.\n- Recoverability: nightly snapshots to object store + a weekly full export to their SAN. For volumes we consider \"durable sharded state\" set reclaimPolicy=Retain and schedule an operator-driven copy.\n- Ops checklist: multipath/iSCSI tuning, kernel SCSI timeout settings, udev rules for NVMe renaming, and a node drain playbook that safely drains local-PV pods with pre-copy snapshots.\n\nOpen items we need from you:\n1) Expected steady-state QPS and 99th percentile request size per tenant (we used conservative estimates in the spreadsheet).\n2) Do you want us to manage the snapshot operator, or do you prefer an ops-runbook so your team runs it?\n\nIf this looks good, we can convert to an actionable runbook and share an implementation timeline.\n\nBest,\nSoojin\nPrivate Deployments PM\nRedwood Inference\n\nOn Thu, Jul 29, 2027 at 11:03, Dev Patel <dev_patel@redwood.example.com> wrote:\n> Soojin —\n> \n> Quick notes after skimming the spreadsheet and their queue profile:\n> - Node pools: Recommend split GPU node pools per tenant for strong isolation. Use a small shared CPU node pool for control-plane services and batch jobs. That keeps noisy neighbor risk lower and allows per-tenant autoscaling.\n> - Scheduling: Apply node taints (gpu=dedicated:NoSchedule) + per-tenant tolerations and nodeSelector labels like tenant=pegasus-<name>. This makes admission/audit simpler.\n> - Eviction: local SSD-backed pods should use emptyDir or local PersistentVolumes depending on durability needs. For cache-only volumes (rebuildable from snapshot), set reclaimPolicy=Delete and tolerate eviction. For shards that are expensive to rebuild, use ReclaimPolicy=Retain + scheduled replication.\n> - Storage: prefer CSI local PVs + a lightweight agent that snapshots to their shared object store every 15 minutes. Avoid NFS for high-I/O KV cache.\n> \n> —Dev\n
From: John Smith <john.smith@pegasus.ai>\nTo: Soojin Lee <soojin_lee@redwood.example.com>\nCc: Infra Team <infra@pegasus.ai>, contracts@pegasus.ai\nDate: 2027-07-31T08:45:00Z\nSubject: Re: Tenant isolation & local SSD sizing guidance (proposal)\n\nThanks Soojin — this is very helpful. A few clarifying questions from our infra team:\n\n1) For the \"1–2 TB NVMe\" recommendation, what's the expected rebuild time if a node dies and we have to repopulate the cache from object store? We need to ensure transient QPS is acceptable during rebuild.\n2) On reclaimPolicy: if we use Retain for expensive shards, can Redwood provide a policy for automated cleanup if the tenant is decommissioned? We want to avoid orphaned disks consuming capacity.\n3) For SAN fallback, do you have a tested sequence for failover (i.e., operate reads from SAN while degraded, then attach local NVMe when ready)?\n\nWe'd prefer Redwood to manage the snapshot operator initially while our team ramps up. Also happy to share our steady-state telemetry next week.\n\nRegards,\nJohn Smith\nHead of Infrastructure\nPegasus AI\nAttachments: acceptance-checklist-draft.docx (referenced)\n
From: Hannah Schmitt <hannah_schmitt@redwood.example.com>\nTo: John Smith <john.smith@pegasus.ai>, Soojin Lee <soojin_lee@redwood.example.com>, Dev Patel <dev_patel@redwood.example.com>\nCc: contracts@pegasus.ai\nDate: 2027-08-02T16:05:00Z\nSubject: Re: Tenant isolation & local SSD sizing guidance (proposal) — decisions and next steps\n\nJohn / team —\n\nThanks for the questions — answering inline and summarizing next steps.\n\nQ1 (rebuild time): For a 1.5 TB NVMe working set, a cold rebuild from object store (S3-equivalent) will be limited by network bandwidth. On a 10 Gbps link expect ~3–4 hours to stream data end-to-end; with our operator we can prioritize critical shards first and get service-critical traffic back in <30 minutes for the hottest keys. We'll add a prioritization plan to the runbook.\n\nQ2 (orphan cleanup): We'll provide an operator-level lifecycle policy: when tenant deletion is confirmed via a a deletion CRD, the operator performs a final snapshot to object store and then will either Delete or Retain based on a tenant-level tag. We'll include a 90-day retention default and a manual cleanup playbook.\n\nQ3 (SAN fallback): Recommended sequence: 1) Mark node as degraded; 2) switch reads to SAN via our sidecar (read-through), 3) schedule snapshot/restoration to local NVMe during low traffic windows, 4) validate checksum, 5) reattach local PV and switch back. We'll provide a runbook with exact kubectl and admin steps.\n\nNext steps / owners:\n- Redwood to produce runbook + operator config (Soojin) — delivery by 2027-08-10.\n- Redwood to provision operator deployment and initial snapshot scheduling (Dev) — coordinate on access by 2027-08-12.\n- Pegasus to share steady-state telemetry and 95/99 p99 request sizes (John) — by 2027-08-06.\n\nI'll convert this thread into a short runbook doc and a JIRA ticket (INFRA-4482) this week and drop the Drive link once it's ready.\n\nBest,\nHannah Schmitt\nLead Solutions Engineer, Redwood Inference\n