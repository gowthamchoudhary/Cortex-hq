EchoHealth devbench: SDK quickstart, auth & local sample plan

From: Alex Rivera <alex.rivera@echohealth.com>
To: Dylan Brooks <dylan_brooks@redwood.com>
Cc: Priya Shah <pshah@echohealth.com>
Date: Mon, 19 Apr 2027 15:02:00 -0700
Subject: Re: EchoHealth devbench: SDK quickstart, auth & local sample plan

Hi Dylan —

Thanks for the demo earlier. A couple of quick clarifying questions from the engineering side before we lock the next call:

- We run inference in a hybrid setup (VPC-hosted + on-prem preprocessing). Can the Redwood SDK pick the private endpoint via environment variables at runtime, or does that require rebuilding the client?
- For role-based access: can tokens be scoped to route-level permissions (e.g., embeddings only for the ingestion worker)?
- Do you have a recommended local test pattern for sequence-length heavy workloads (we commonly trim to 1–2k tokens)?

Also attaching a short internal checklist we used during the call (see inline).

—Alex

-- original message below --

From: Dylan Brooks <dylan_brooks@redwood.com>
To: Alex Rivera <alex.rivera@echohealth.com>, Priya Shah <pshah@echohealth.com>
Date: Mon, 19 Apr 2027 13:15:00 -0700
Subject: EchoHealth devbench: SDK quickstart, auth & local sample plan

Alex/Priya — thanks for the time today. Recap and next steps from our side (engineering-focused):

1) Quickstarts we’ll hand over by EOD tomorrow: minimal Python + TS samples that cover auth, routing, embeddings, and a tiny tool-calling example.
2) Local test plan: how to run the SDK pointing at (a) hosted sandbox, (b) private endpoint (VPC), and (c) a mocked local server for CI.
3) Auth & scoping: we'll include examples for scoped API keys + a short note on integrating with your KMS for signing.
4) Batching/kv-cache guidance for long sequences — we’ll include perf knobs and a script to generate synthetic payloads that match your token distribution.

Can you confirm a few things so we tailor the sample apps?
- Which languages are first priority (you mentioned Python/TypeScript)?
- Any internal restrictions on sample code (no external internet access for CI)?
- Who should be on the technical follow-up call (engineer + security contact)?

I'll loop in Marissa and Karthik to ship the sample kit and the mutual action plan.

Thanks,
Dylan

-----
From: Marissa Cole <marissa_cole@redwood.com>
To: Dylan Brooks <dylan_brooks@redwood.com>, Karthik Iyer <karthik_iyer@redwood.com>
Cc: Alex Rivera <alex.rivera@echohealth.com>, Priya Shah <pshah@echohealth.com>
Date: Tue, 20 Apr 2027 09:30:00 -0700
Subject: Re: EchoHealth devbench: SDK quickstart, auth & local sample plan

Looping in the SDK team. Drafting the artifacts below — please review and flag anything missing: 

Attachments delivered (zipped): sdk_quickstart.zip
- python/
  - quickstart.py (minimal generation + streaming example)
  - auth_examples.md (API key scoping + env var patterns)
  - local_mock/ (small Flask app to emulate private endpoint)
- typescript/
  - sample-app/ (Next.js toy UI showing structured output + tool call)
  - auth.ts (scoped token wrapper example)
- infra/
  - docker-compose.yml (starts mock endpoint + local kv-cache)
  - run_locally.md (step-by-step)
- perf/
  - token_generator.py (create synthetic inputs matching EchoHealth distribution)

Key notes for EchoHealth: 
- Private endpoint selection: SDK honors REDWOOD_ENDPOINT and REDWOOD_REGION env vars at runtime; no rebuild required. See run_locally.md for examples.
- Route-level scoping: tokens can be scoped by route via policy mapping (we include sample mapping and server-side enforcement notes). Example in auth_examples.md shows key:routes = ['embeddings:write', 'generation:read'].
- Local testing: the docker-compose includes a small mock that accepts the same REST surface as the hosted API; you can run the Python quickstart pointed at http://localhost:8080 to validate CI.

Next steps suggested (we put in MAP spreadsheet):
- Technical walkthrough with your infra/security (30–45m) — propose Wed 21 Apr 10:00 PT.
- Shared evaluation: EchoHealth runs the Python sample against the mock for smoke tests + perf script to generate 10k tokens/day distribution.
- Procurement: Priya to confirm any controls required for token scoping.

If that looks good I’ll send a separate email with links to the individual files in Drive and a short video walkthrough.

—Marissa

> On Apr 20, 2027, at 09:30, Karthik Iyer <karthik_iyer@redwood.com> wrote:
>
> Thanks — added a tiny reranker example (rerank/README.md) to the zip. Reranker uses embeddings + a lightweight cosine match so you can exercise both endpoints in the sample.
>
> Also added a note on quantization/perf tradeoffs in perf/README.md (how to tune for 1k–2k sequences).
>
> -Karthik

-----
From: Priya Shah <pshah@echohealth.com>
To: Marissa Cole <marissa_cole@redwood.com>, Alex Rivera <alex.rivera@echohealth.com>
Cc: Dylan Brooks <dylan_brooks@redwood.com>, Karthik Iyer <karthik_iyer@redwood.com>
Date: Wed, 21 Apr 2027 09:45:00 -0700
Subject: Re: EchoHealth devbench: SDK quickstart, auth & local sample plan

Marissa/Dylan/Karthik — this looks great and actionable. Two quick asks before the walkthrough: 

1) Can you add a brief note in run_locally.md describing how the mock simulates rate limiting and fallback to a hosted route (we want to validate our client's fallback behavior)?
2) In auth_examples.md please show an example of credential rotation — our security team will want to see an automated rotation script.

Wed 10:00 PT works for our infra lead (I'll invite them). We’ll aim to run the Python sample against the mock ahead of time and send any blockers.

Thanks,
Priya

[Attachment: mutual_action_plan_ECHO-1345.xlsx (spreadsheet, 46KB)]

